# Secrets & local state — never publish these (config.yaml holds real API keys).
config.yaml
config.yaml.bak
config.yaml.tmp
.env
.env.local
dashboard/.env.local
data/
*.sqlite
*.sqlite-journal
*.log

# Ship dashboard/.next/ (pre-built) so first run skips the build step.
# Only exclude the Turbopack dev cache (dev/) which is 680 MB and unused in prod.
dashboard/.next/dev/
tests/
*.test.ts
.claude/
.gstack/
.omo/
CLAUDE.md
logo-previews/
